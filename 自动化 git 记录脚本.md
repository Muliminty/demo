## 2026-02-02
``` 
- fix: 处理组件数据时添加可选链操作符
  - 在 AddOrEditBees 函数中，使用可选链操作符确保 componentData 存在后再进行遍历，避免潜在的错误。
    
- feat: 更新构建脚本以改进压缩文件命名格式
  - 修改 zip 文件名生成逻辑，使用更精确的时间戳格式，包括小时、分钟和秒，以确保文件名的唯一性和可读性。

- style: 更新任务卡片按钮样式以适应图片显示
  - 将任务卡片按钮的图片宽度设置为 100%。
  - 添加 object-fit 属性以确保图片按比例缩放并保持可见性。

- style: 调整任务卡片按钮样式

- feat: 更新样式配置和任务描述逻辑
  - 在 EleConfigWrapper 的样式文件中，添加了对搜索选择器的光标样式设置。
  - 在 StyleConfig 组件中，优化了任务描述显示逻辑，增加了对 IMAGE 类型的高度设置。

- feat: 支持草稿状态的活动详情获取
  - 在获取活动详情的请求中，添加了对草稿状态的支持，通过 URL 参数传递 isDraft。
  - 更新了活动列表卡片的跳转逻辑，确保在访问草稿活动时正确传递参数。

- fix: 优化构建脚本的重命名逻辑和输出信息

- refactor: 优化构建脚本的注释和输出信息 
  - 修改了 build.js 中的注释格式，使用数字替代表情符号，提升了可读性。
  - 简化了错误和日志输出信息，去除了表情符号，使其更加简洁明了。
  - 更新了压缩文件命名规则，调整了 zip 文件名格式。

- feat: 添加构建和压缩脚本支持不同环境
  - 在 package.json 中新增了 zip:test、zip:uat 和 zip:pro 命令，用于构建和压缩不同环境的产物。
  - 新增了 scripts/build.js 脚本，支持自动识别构建环境，清理旧目录，并将构建结果重命名和压缩为 zip 包。

- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券

- style: 调整任务卡片按钮样式

- feat: 更新样式配置和任务描述逻辑
  - 在 EleConfigWrapper 的样式文件中，添加了对搜索选择器的光标样式设置。
  - 在 StyleConfig 组件中，优化了任务描述显示逻辑，增加了对 IMAGE 类型的高度设置。

- feat: 添加资讯组件及其验证逻辑
  - 在 DataVerification.js 中新增了资讯组件的必填配置和特殊校验逻辑，确保至少添加一条资讯。
  - 在 DragSelection 组件的 listData.js 中添加了资讯组件的配置。
  - 新增了 News 组件及其样式，支持资讯的搜索、添加和展示功能。
  - 更新了 PreviewComponents 以支持新添加的资讯组件。

- feat: 支持草稿状态的活动详情获取
  - 在获取活动详情的请求中，添加了对草稿状态的支持，通过 URL 参数传递 isDraft。
  - 更新了活动列表卡片的跳转逻辑，确保在访问草稿活动时正确传递参数。

- fix: 优化构建脚本的重命名逻辑和输出信息

- fix: 更新 zip:test 命令并改进 zip 文件命名格式

- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券

- refactor: 优化构建脚本的注释和输出信息
  - 修改了 build.js 中的注释格式，使用数字替代表情符号，提升了可读性。
  - 简化了错误和日志输出信息，去除了表情符号，使其更加简洁明了。
  - 更新了压缩文件命名规则，调整了 zip 文件名格式。

  
- feat: 添加构建和压缩脚本支持不同环境
  - 在 package.json 中新增了 zip:test、zip:uat 和 zip:pro 命令，用于构建和压缩不同环境的产物。
  - 新增了 scripts/build.js 脚本，支持自动识别构建环境，清理旧目录，并将构建结果重命名和压缩为 zip 包。

```

## 2026-02-03
```
- feat: 实现资格列表的分页加载和下拉滚动加载功能
  - 在 Task 组件中添加资格列表的分页支持，允许用户加载更多资格。
- 优化资格列表的获取逻辑，使用 useCallback 和 useEffect 管理状态。
- 在 PlayMethod 组件中实现资格下拉框的滚动加载，提升用户体验。


- feat: 更新新闻组件图标和删除确认功能
  - 更换新闻组件的图标以提升视觉一致性。
- 在删除新闻时添加确认弹窗，增强用户操作的安全性。


- feat: 更新新闻组件样式和功能
  - 添加了新的图标以增强用户交互体验。
- 调整了输入框和日期选择器的样式，使其宽度适应容器。
- 优化了文章长度输入区域的布局，增加了边框和间距。
- 更新了搜索结果展示，添加了空状态提示，提升了视觉效果。


- feat: 添加新闻组件类型及样式调整
  - 在组件类型过滤中新增 'NEWS' 类型，以支持新闻组件的展示。
- 移除新闻组件的背景和边框样式，简化视觉效果。


- feat: 添加组件类型过滤功能
  - 更新组件列表，使用 ALLOWED_COMPONENT_TYPES 进行类型过滤，仅展示允许的组件类型。
- 修改 list 变量为可变类型，以支持过滤操作。


- feat: 更新 SZ_COIN 奖品类型的配置和表单项逻辑
  - 修改 SZ_COIN 奖品类型的图像和描述，确保其正确显示为积分。
- 在奖品表单项中添加对 SZ_COIN 类型的支持，确保表单逻辑的完整性。


- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券
  

- feat: 添加上证权益奖品类型及相关配置
  - 在奖品选择和类型表单中新增 SZ_RED、SZ_COIN 和 SZ_COUPON 奖品类型。
- 更新奖品类型映射和默认数据，确保新类型的正确显示和使用。
- 在枚举文件中添加上证权益相关的选项。


- feat: 添加链接字段以支持精确搜索
  - 在 MiniLink 组件中新增链接字段，允许用户输入短链接或原链接进行精确搜索。
- 更新表单项属性以设置占位符提示用户输入内容。


- feat: 添加输入框字符限制和计数显示
  - 在短链名称和原链接的输入框中添加 maxLength 属性，限制输入字符数。
- 启用 showCount 属性，以便用户实时查看已输入字符数。
- 移除不必要的 key 属性 'create_time'，简化数据表格配置。

- feat: 添加链接字段以支持精确搜索
  - 在 MiniLink 组件中新增链接字段，允许用户输入短链接或原链接进行精确搜索。
- 更新表单项属性以设置占位符提示用户输入内容。


- feat: 添加输入框字符限制和计数显示
  - 在短链名称和原链接的输入框中添加 maxLength 属性，限制输入字符数。
- 启用 showCount 属性，以便用户实时查看已输入字符数。
- 移除不必要的 key 属性 'create_time'，简化数据表格配置。

```

## 2026-02-04
```
- feat: 更新奖品来源变化处理逻辑
  - 在 PrizeSelectFormItem 组件中，将 productPlatformType 的初始值修改为 null，以更好地反映未选择状态。
- 添加 useEffect 钩子，监听奖品来源变化，自动更新 productPlatformType，确保根据不同奖品来源正确设置平台类型。
- 优化奖品选择逻辑，移除不必要的状态更新，提升代码可读性和维护性。


- feat: 增加 SZ_RIGHT 类型的库存校验逻辑
  - 在 StorageFormItem 组件中添加对 SZ_RIGHT 类型奖品的特殊处理，确保在库存校验时不进行限制。
- 更新相关注释以提高代码可读性，明确逻辑处理。


- feat: 更新 SZ_COUPON 奖品信息获取逻辑
  - 在 PrizeSelectFormItem 组件中，修改 SZ_COUPON 类型的奖品信息获取方式，调用 getCouponList API 根据 discountCode 查询优惠券信息。
- 增强错误处理逻辑，确保在 API 调用失败时能够回退到本地表单数据。
- 更新注释以清晰描述各步骤的处理逻辑，提升代码可读性。


- feat: 优化奖品详情获取逻辑和状态同步
  - 更新 PrizeSelectFormItem 组件中的奖品详情获取逻辑，确保根据奖品类型正确处理 SZ_COUPON 和其他商品类型。
- 增强状态管理，确保表单数据与远程获取的商品信息保持一致，提升用户体验。
- 添加详细注释，明确各步骤的处理逻辑和数据流动。


- feat: 优化 SZ_COUPON 奖品选择逻辑和状态管理
  - 更新 PrizeSelectFormItem 组件中的奖品选择逻辑，确保 SZ_COUPON 类型的奖品信息能够正确回填。
- 使用 Form.useWatch 监控表单数据变化，提升用户体验。
- 增强状态管理，确保在表单数据更新后，奖品信息能够及时反映。


- feat: 添加 SZ_COUPON 图标并优化奖品选择逻辑
  - 在 PrizeSelectFormItem 组件中引入 SZ_COUPON 图标，提升用户界面的一致性。
- 更新奖品选择逻辑，针对 SZ_COUPON 类型进行特殊处理，确保用户在选择奖品时的体验流畅。
- 调整 API 调用和状态管理，确保不同类型奖品的正确处理。


- feat: 添加优惠券列表支持和优化奖品选择逻辑
  - 在 PrizeSelectFormItem 组件中引入 getCouponList 方法，以支持根据优惠券名称和代码获取优惠券列表。
- 优化奖品选择逻辑，区分商品和优惠券的 API 参数，确保正确处理不同类型的奖品。
- 更新状态管理，提升用户体验和数据展示的准确性。


- feat: 增强资格列表展示逻辑和任务事件处理
  - 在 Task 组件中引入 useMemo 优化资格列表的展示逻辑，支持 customer-group 类型的事件。
- 更新资格列表的获取方式，确保正确显示和处理任务事件的 ID 和名称。
- 调整 PlayMethod 组件中的资格列表传递，提升用户体验。


- feat: 优化任务事件处理和资格值规范化
  - 在 Task 组件中增强任务事件处理逻辑，支持 customer-group 类型的事件。
- 在 PlayMethod 组件中添加资格值的规范化功能，确保正确格式化 customer-group 的资格值。
- 更新资格下拉框的值显示，提升用户体验。


```

## 2026-02-05

```
- feat: 更新活动登录类型和参与方式
  - 将活动登录类型从“ACCOUNT”更改为“PHONE”，以支持新的登录方式。
- 修改参与活动的设置，将“isAppOnly”从 true 更新为 false，允许更多参与渠道。


- feat: 注释掉不再使用的产品类型选项
  - 将“股票”和“期货”选项注释掉，以简化产品类型配置，保持代码整洁。


- feat: 移除猜涨跌排行榜选项
  - 从排行榜选项中移除“猜涨跌排行榜”条目，简化组件配置。


- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券
  

- feat: 添加代码注释、ESLint 规则及命名规范文档
  - 新增注释规则文档，强调注释应说明设计思路与原因。
- 新增 ESLint 规则文档，确保生成或修改代码时遵循项目的 ESLint 规范。
- 新增命名规范文档，规定组件、Hooks、文件及样式的命名方式。


- Revert "feat: 添加奖品选择功能及金额处理"
  This reverts commit 2448542d7649a456057fdb4292c8792d7be6f6c9.


- feat: 添加奖品选择功能及金额处理
  - 在 EditModal 组件中引入 PrizeSelectFormItem 以支持奖品选择。
- 增加当前选择奖品金额的状态管理，并在更新客户信息时提交奖品金额。
- 初始化已有奖品金额以便于编辑时显示。


- feat: 更新新闻组件以增强搜索功能和展示逻辑
  - 在新闻配置组件中，添加搜索完成后的处理函数，优化搜索逻辑。
- 修改新闻预览组件，限制显示的股票标签数量，提升界面整洁度。
- 更新元数据展示，使用作者信息替代来源，增强信息的相关性和可读性。


- feat: 重构新闻搜索组件以使用表单管理搜索条件
  - 将搜索条件管理从状态提升到表单，简化搜索逻辑并提高可读性。
- 添加文章长度筛选功能，允许用户根据最小和最大文章长度进行搜索。
- 优化搜索请求参数构建，确保与新表单结构一致，提升用户体验。


- feat: 增强新闻搜索功能，添加文章长度筛选
  - 在新闻配置组件中添加文章长度的最小和最大值输入框，支持用户根据文章长度进行搜索。
- 更新状态管理，确保新的搜索条件与现有逻辑一致，提升搜索功能的灵活性和用户体验。


- feat: 增强新闻搜索功能与结果加载逻辑
  - 添加搜索结果每页条数常量，优化搜索请求的分页处理。
- 实现加载更多搜索结果的功能，支持滚动加载。
- 更新搜索结果展示，增加加载状态和无更多结果提示。
- 改进样式以提升用户体验，确保搜索结果的可读性和交互性。


- refactor: 更新新闻组件数据处理逻辑
  - 修改新闻配置组件以直接使用 articles 属性，简化数据访问。
- 移除不必要的 propValue 包装，提升代码可读性和维护性。
- 更新相关组件以确保一致性，优化数据流动和状态管理。


- refactor: 重构新闻配置组件以简化搜索逻辑
  - 移除不必要的状态和属性，简化搜索条件的管理。
- 更新搜索处理逻辑，使用本地状态而非 propValue 进行搜索条件的存储。
- 增强代码可读性，优化日期选择和关键词输入的处理方式。

- feat: 添加代码注释、ESLint 规则及命名规范文档
  - 新增注释规则文档，强调注释应说明设计思路与原因。
- 新增 ESLint 规则文档，确保生成或修改代码时遵循项目的 ESLint 规范。
- 新增命名规范文档，规定组件、Hooks、文件及样式的命名方式。


- Revert "feat: 添加奖品选择功能及金额处理"
  This reverts commit 2448542d7649a456057fdb4292c8792d7be6f6c9.


- feat: 添加奖品选择功能及金额处理
  - 在 EditModal 组件中引入 PrizeSelectFormItem 以支持奖品选择。
- 增加当前选择奖品金额的状态管理，并在更新客户信息时提交奖品金额。
- 初始化已有奖品金额以便于编辑时显示。

- Merge branch 'feature-4.0上海证券' of ssh://gitlab.jiniutech.com:2222/hd/web-project/management-platform into feature-4.0上海证券
  

- fix(deploymentConfig): 更新私有化部署标识为 true
  

- fix(build): 更新 zip:test 脚本输出目录和压缩文件命名格式
  

- feat(ActivityReview): 动态构建活动详情页路径
  - 在 ActivityReview 组件中引入 BASE_PATH 配置，根据部署环境动态构建活动详情页的 URL，提升了灵活性和适应性。
- 该更改确保在不同部署环境下，活动详情页的链接能够正确生成。


```

## 2026-02-06
```
- fix(deploymentConfig): 修正活动链接格式以去除尾部斜杠
  - 更新私有化部署配置中的活动链接，去掉 dev 和 test 环境的 URL 尾部斜杠，确保链接格式一致性。


- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券
  

- refactor(PreviewModal): 注释掉付费类型字段以简化展示
  - 将 PreviewModal 组件中的付费类型字段注释掉，减少界面复杂性。
- 该更改旨在优化用户体验，后续可根据需求重新启用该字段。

- style: 更新样式以确保选择器光标始终为指针
  - 修改了 .ant-select-show-search 选择器的样式，确保光标在特定条件下始终显示为指针。
- 使用 !important 以增强样式的优先级，确保一致的用户体验。


- feat: 根据图片高度动态调整组件高度
  - 在 StyleConfig 组件中，添加逻辑以根据上传的 goButton 图片高度动态更新组件的高度。
- 处理图片加载成功和失败的情况，确保在图片加载失败时使用默认高度。
- 增强了组件的灵活性和用户体验。


- refactor: 更新客户列表标题并移除不必要的表单项
  - 将客户管理标题更改为客户列表，以更准确地反映页面内容。
- 移除账号名称的表单项渲染函数，简化组件结构，提高可读性。


- refactor: 注释掉不必要的表单项和按钮代码
  - 将活动参与人数限制的表单项和相关按钮代码注释掉，以简化组件结构。
- 该更改有助于提高代码的可读性和维护性，同时保留了原有逻辑以备后续使用。


- refactor: 优化 AddOrEditBees 函数以使用数据副本处理组件状态和坐标
  - 在 AddOrEditBees 函数中，创建数据副本以避免直接修改原始数据，确保数据完整性。
- 更新计算绝对坐标的逻辑，处理 GROUP 组件的旋转情况。
- 清理和重构代码，提高可读性和维护性。


```

## 2026-02-10
```
- feat: 优化竞猜配置模态框的产品搜索与加载功能
  - 在 ConfigModal 组件中实现了分页加载和搜索功能，提升了产品选择的用户体验。
- 使用 useSelectLoadMore Hook 处理下拉框的滚动加载，支持防抖搜索。
- 重置相关状态以确保在模态框关闭时清空搜索和产品列表，提升了组件的可用性。


- feat: 添加下拉框滚动加载更多功能
  - 新增 useSelectLoadMore 自定义 Hook，封装了下拉框滚动触底加载更多的逻辑，包括防抖和卸载清理。
- 在相关组件中集成 useSelectLoadMore，优化了资格下拉框的加载体验。
- 更新了 GuessUnderlyingConfig 组件的代码格式，提升了可读性。


- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券
  

- refactor: 优化 ImgList 组件代码格式
  - 调整了代码格式，确保一致性和可读性，包括空格和换行的使用。
- 更新了点击事件处理逻辑，简化了组件的结构，提升了代码的清晰度。


- feat: 重置新增模式下的表单字段
  - 在配置模态框中新增逻辑，确保在新增模式下表单字段重置，避免保留上次编辑或取消的缓存，提升用户体验。


- feat: 添加时间一致性校验功能
  - 在新增配置模态框中实现时间一致性校验，确保新增标的的时间设置与现有标的保持一致。
- 提供错误提示，指导用户正确设置时间，提升用户交互体验。


- feat: 更新竞猜配置模态框
  - 优化搜索功能，确保用户可以按名称、代码及原始数据进行搜索。
- 在选择框中添加 onChange 事件，清除相关字段的值，提升用户交互体验。


- feat: 更新活动选项显示格式
  - 修改活动选项的显示格式，将活动名称与ID结合显示，提升用户对活动的识别度。


- feat: 更新主题配置和文本样式
  - 在配置文件中为 Ant Design 添加了主题设置，定义了主要按钮文字颜色。
- 在 TextConfig 组件中移除了文本阴影的默认值，优化了样式逻辑。
- 更新了管理后台图标的颜色样式，提升了界面一致性。


- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券
  

- feat: 更新基金编辑模态框标题
  - 修改基金编辑模态框的标题，根据操作类型动态显示“新增基金”或“编辑基金”，提升用户体验。


- feat: 增强预警管理界面用户体验
  - 在预警管理界面添加警告提示，提醒用户配置活动和库存预警规则。
- 更新奖品选择框的占位符，明确提示用户选择内容。
- 为峰值预警和剩余库存预警标签添加工具提示，提供额外信息以帮助用户理解功能。


- feat: 优化物流公司选择和快递单号输入
  - 在物流公司选择框中添加搜索功能，支持按名称过滤选项，提升用户体验。
- 更新快递单号输入框，添加占位符提示用户输入快递单号。


- refactor: 更新颜色属性结构
  - 将颜色属性从 style.textColor 移动到 colors.textColor，以统一组件的颜色管理。
- 在相关组件中调整引用，确保样式一致性。
- 优化代码格式，提升可读性。


- feat: 添加清除数据功能
  - 在 FontImageObj.js 中添加清除数据图标和悬停图标。
- 在 TextConfig.jsx 中实现清除数据的逻辑，重置文本样式为默认设置，提升用户体验。


- style: 文本默认垂直居中
  

- style: 更新兑换链接输入框以支持最大长度和字符计数
  - 在 GoodsModal 组件的 RenderFormItems 中，修改了兑换链接输入框，添加了 maxLength 属性和 showCount 属性，以限制输入长度并显示字符计数，提高用户体验。


- refactor: 移除 GoodsList 中不必要的 key 属性
  

- refactor: 修复预消耗排序不了
  

- style: 更新拼图配置模态框的错误提示样式
  - 修改了 .ant-form-item-explain 的样式，使错误提示信息在同一行展示，并设置最小宽度以提高可读性。


- style: 评论组件去除背景色
  

- feat: 优化任务组件的加载更多逻辑
  - 在 Task 组件中，新增 resolveHasMore 函数以判断是否还有更多数据，提升代码可读性。
- 更新 loadMoreEligibility 和 fetchEligibilityPage 方法，使用 resolveHasMore 函数来设置 hasMore 状态，确保逻辑一致性。
- 在 PlayMethod 组件中，添加下拉菜单渲染逻辑，显示已加载全部的提示信息，改善用户体验。


- feat: 添加嗨币奖品类型表单项
  - 在 PrizeFormItems 组件中，新增嗨币奖品类型的表单项，包括积分分值和积分描述的输入框。
- 更新 PrizeTypeFormItem 组件中的 SZ_COIN 奖品类型，修正字段名称以保持一致性。
- 修改 Task 组件的 PAGE_SIZE 常量，首次加载与下滑加载均查询 100 条数据，以优化性能。


- feat: 更新新闻组件以支持动态股票信息显示
  - 在 News 组件中，添加了示例文章数据以便于开发和测试。
- 优化了股票信息的显示逻辑，确保根据股票名称长度动态调整显示的股票数量。
- 更新了样式以改善标签的布局和视觉效果，增强用户体验。


- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券
  

- feat: 添加导出列表标题
  - 在 ExportList 组件中，更新 PageContainer 的标题为“导出列表”，以增强页面的可读性和用户体验。


- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券
  

- feat: 更新奖品类型隐藏逻辑以包含 SZ_COIN
  - 在 EditPrizeModal 组件中，更新隐藏模型的条件，新增 SZ_COIN 奖品类型，以确保正确处理该类型的奖品显示逻辑。


- feat: 推广二维码支持点击下载
  
- feat: 更新数据组件样式
  - 调整数据组件的宽度从 90 到 130，提升视觉效果。
- 修改数据组件的高度从 20 到 27，以改善布局和可读性。


- feat: 更新组件文本样式和对齐方式
  - 将组件列表中的字体大小从 12 增加到 14，提升可读性。
- 修改默认文本对齐方式为居中，确保一致的视觉效果。
- 更新相关组件的状态管理，确保文本对齐方式在不同组件中保持一致。


- feat: 增强自定义积分组件的表单逻辑
  - 添加了对模态框打开时表单字段的自动填充逻辑，确保用户在重新打开模态框时能看到上次提交的值。
- 引入 useRef 钩子以管理表单状态，提升了组件的性能和用户体验。


- fix: 修复自定义押注上限和下限的验证逻辑
  - 优化了自定义押注上限和下限的验证规则，确保输入值的合理性。
- 添加了对押注单位的验证，确保上限和下限能被单位整除，提升了表单的准确性和用户体验。


- fix: 修复自定义押注上限和下限的验证逻辑
  - 优化了自定义押注上限和下限的验证规则，确保输入值的合理性。
- 添加了对押注单位的验证，确保上限和下限能被单位整除，提升了表单的准确性和用户体验。


- feat: 添加 Tooltip 背景色配置
  - 在主题配置中新增 colorBgSpotlight 属性，定义 Tooltip 的背景色为白色，提升界面可读性和用户体验。


- refactor: 优化 ImgList 组件代码格式
  - 调整了代码格式，确保一致性和可读性，包括空格和换行的使用。
- 更新了点击事件处理逻辑，简化了组件的结构，提升了代码的清晰度。


- feat: 重置新增模式下的表单字段
  - 在配置模态框中新增逻辑，确保在新增模式下表单字段重置，避免保留上次编辑或取消的缓存，提升用户体验。


- feat: 添加时间一致性校验功能
  - 在新增配置模态框中实现时间一致性校验，确保新增标的的时间设置与现有标的保持一致。
- 提供错误提示，指导用户正确设置时间，提升用户交互体验。


- feat: 更新竞猜配置模态框
  - 优化搜索功能，确保用户可以按名称、代码及原始数据进行搜索。
- 在选择框中添加 onChange 事件，清除相关字段的值，提升用户交互体验。


- feat: 更新活动选项显示格式
  - 修改活动选项的显示格式，将活动名称与ID结合显示，提升用户对活动的识别度。


- feat: 更新主题配置和文本样式
  - 在配置文件中为 Ant Design 添加了主题设置，定义了主要按钮文字颜色。
- 在 TextConfig 组件中移除了文本阴影的默认值，优化了样式逻辑。
- 更新了管理后台图标的颜色样式，提升了界面一致性。


- feat: 更新基金编辑模态框标题
  - 修改基金编辑模态框的标题，根据操作类型动态显示“新增基金”或“编辑基金”，提升用户体验。


- feat: 增强预警管理界面用户体验
  - 在预警管理界面添加警告提示，提醒用户配置活动和库存预警规则。
- 更新奖品选择框的占位符，明确提示用户选择内容。
- 为峰值预警和剩余库存预警标签添加工具提示，提供额外信息以帮助用户理解功能。


- feat: 优化物流公司选择和快递单号输入
  - 在物流公司选择框中添加搜索功能，支持按名称过滤选项，提升用户体验。
- 更新快递单号输入框，添加占位符提示用户输入快递单号。


- refactor: 更新颜色属性结构
  - 将颜色属性从 style.textColor 移动到 colors.textColor，以统一组件的颜色管理。
- 在相关组件中调整引用，确保样式一致性。
- 优化代码格式，提升可读性。


- feat: 添加清除数据功能
  - 在 FontImageObj.js 中添加清除数据图标和悬停图标。
- 在 TextConfig.jsx 中实现清除数据的逻辑，重置文本样式为默认设置，提升用户体验。


- style: 文本默认垂直居中
  

- style: 更新兑换链接输入框以支持最大长度和字符计数
  - 在 GoodsModal 组件的 RenderFormItems 中，修改了兑换链接输入框，添加了 maxLength 属性和 showCount 属性，以限制输入长度并显示字符计数，提高用户体验。


- refactor: 移除 GoodsList 中不必要的 key 属性
  

- refactor: 修复预消耗排序不了
  

- style: 更新拼图配置模态框的错误提示样式
  - 修改了 .ant-form-item-explain 的样式，使错误提示信息在同一行展示，并设置最小宽度以提高可读性。


- style: 评论组件去除背景色
  

- feat: 添加导出列表标题
  - 在 ExportList 组件中，更新 PageContainer 的标题为“导出列表”，以增强页面的可读性和用户体验。


- feat: 推广二维码支持点击下载
  

```

## 2026-02-11

```
- feat: 增强文章组件点击处理，以便导航至新闻详情
  

- refactor: 删除 log
  

- feat: 添加构建和压缩脚本，支持多环境构建
  

- feat: 添加股票中性状态样式并优化股票报价显示逻辑
  

- feat: 增强文章组件中的股票报价获取和显示。
  

- Merge branch 'feature-4.0上海证券' into feature-4.0上海证券-zxq
  

- feat: 更新文章组件样式和逻辑，优化股票信息显示
  
- fix: 更新奖品选择逻辑以支持 externalId
  - 修改了奖品选择逻辑，允许通过 discountCode 和 externalId 查询 SZ_COUPON 类型的优惠券信息。
- 优化了获取奖品详情的流程，确保在未找到优惠券时能够回填已保存的奖品信息。
- 确保表单与接口返回的数据保持一致，提升了用户体验。


- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券
  

- fix: 更新奖品详情获取逻辑
  - 在获取奖品详情时，新增对奖品来源类型的判断，确保正确传递 isSpacePrivate 参数。
- 优化了获取奖品详情的逻辑，提升了组件的功能性和用户体验。


- fix: 更新 Rank 组件的最小值设置
  - 将 Rank 组件中 showNumber 字段的最小值从 1 调整为 10，以确保输入的合理性和用户体验。


- fix: 调整组件样式和布局
  - 修改了 Rank 组件的样式，移除了用户头像的显示，简化了布局。
- 更新了 rank_container 的高度计算方式，确保组件在不同屏幕上的一致性。
- 调整了 DragSelection 组件中列表项的高度，优化了视觉效果。


- Merge branch 'feature-4.0私有化-dev' into feature-4.0上海证券
  

- fix: 更新导出管理提示信息
  - 修改导出管理组件中的提示信息，提醒用户已完成的导出记录将在7日后自动删除，确保用户及时下载数据。


- refactor: 注释掉不再使用的源选项
  

- feat: 添加文章组件及其样式
  - 新增 ARTICLE 组件，支持文章搜索、加载和展示功能，提升了内容管理的灵活性。
- 实现了文章搜索表单，支持关键词和发布日期范围的筛选。
- 添加了相应的样式文件，优化了组件的视觉效果和用户体验。
- 更新了相关引用，确保组件在活动编辑器中正确加载。

- fix: 调整奖品表格列宽和滚动设置
  - 修改了奖品表格中多个列的宽度设置，确保内容的可读性。
- 更新了表格的水平滚动设置为自适应，提升了用户体验。


- fix: 调整奖品表格列宽和滚动设置
  - 为奖品表格中的多个列添加了宽度设置，确保表格内容的可读性。
- 更新了表格的水平滚动设置，从自适应改为固定宽度，提升了用户体验。


- fix: 更新奖品详情获取逻辑
  - 在获取奖品详情时，新增对奖品来源类型的判断，确保正确传递 isSpacePrivate 参数。
- 优化了获取奖品详情的逻辑，提升了组件的功能性和用户体验。


- fix: 更新 Rank 组件的最小值设置
  - 将 Rank 组件中 showNumber 字段的最小值从 1 调整为 10，以确保输入的合理性和用户体验。


- fix: 调整组件样式和布局
  - 修改了 Rank 组件的样式，移除了用户头像的显示，简化了布局。
- 更新了 rank_container 的高度计算方式，确保组件在不同屏幕上的一致性。
- 调整了 DragSelection 组件中列表项的高度，优化了视觉效果。


- fix: 更新导出管理提示信息
  - 修改导出管理组件中的提示信息，提醒用户已完成的导出记录将在7日后自动删除，确保用户及时下载数据。


- refactor: 注释掉不再使用的源选项
  

```
