# 常用命令

## 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问 http://localhost:3000
```

## Docker 部署

```bash
# 构建并启动服务
docker compose up -d --build

# 查看容器状态
docker compose ps

# 查看日志
docker compose logs -f nextjs
docker compose logs -f nginx

# 停止服务
docker compose down

# 重新构建并启动
docker compose down
docker compose build
docker compose up -d
```

## 修改配置后重新加载

**修改 Next.js 代码后**：
```bash
# 需要重新构建镜像
docker compose down
docker compose build
docker compose up -d

# 或使用一行命令（推荐）
docker compose up -d --build
```

**修改 Nginx 配置后**：
```bash
# 只需重新加载 Nginx 配置（不重启容器，推荐）
docker compose exec nginx nginx -s reload

# 或重启 Nginx 容器
docker compose restart nginx
```

## 其他常用命令

```bash
# 进入 Next.js 容器
docker compose exec nextjs sh

# 进入 Nginx 容器
docker compose exec nginx sh

# 测试 Nginx 配置
docker compose exec nginx nginx -t

# 查看 Next.js 容器日志
docker compose logs -f nextjs

# 查看 Nginx 访问日志
tail -f nginx/logs/app_access.log

# 查看 Nginx 错误日志
tail -f nginx/logs/app_error.log

# 查看容器资源使用情况
docker stats

# 清理未使用的镜像和容器
docker system prune -f
```

## 快速部署脚本

创建 `scripts/deploy.sh`：

```bash
#!/bin/bash
cd /Users/muliminty/project/demo/部署练习/docker_ng_next
echo "🔄 重新部署中..."
docker compose up -d --build
echo "✅ 部署完成！"
docker compose ps
```

使用：
```bash
chmod +x scripts/deploy.sh
./scripts/deploy.sh
```
