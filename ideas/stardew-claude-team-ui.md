# 星露谷物语风格 Claude Code 团队模式 UI

> 创建日期: 2026-02-26
> 状态: 概念设计

## 核心想法

给 Claude Code 的团队模式（Agent Teams）做一个星露谷物语风格的 UI 可视化界面。

启动团队模式后，自动打开浏览器展示像素风格的小人，每个团队成员在工作时有对应的动画状态。

---

## 功能设计

### Agent 状态映射

| Agent 状态 | 小人动作 | 视觉效果 |
|-----------|---------|---------|
| `idle` | 站立/张望 | 偶尔眨眼、打哈欠 |
| `thinking` | 撸胡子/托腮 | 头上冒 "..." 气泡 |
| `working` | 挥斧/浇水/敲键盘 | 持续动作动画 |
| `completed` | 欢呼/举杯 | 星星特效 ✨ |
| `error` | 摔倒/冒烟 | 重新站起的动画 |

### 角色差异化

每个 Agent 可以有不同的像素形象：
- **Plan Agent** → 戴眼镜的学者形象（看书、思考）
- **Explore Agent** → 拿放大镜的探险家
- **Bash Agent** → 拿锤子的工匠
- **Code Agent** → 坐在电脑前的程序员

---

## 技术架构

```
stardew-claude-team/
├── plugin.json              # 插件清单
├── SKILL.md                 # 启动技能
├── server/
│   └── index.py             # 本地HTTP服务器 + MCP
├── ui/
│   ├── index.html           # 星露谷风格UI
│   ├── sprites/             # 像素精灵图
│   │   ├── farmer.png       # 基础角色
│   │   ├── animations/      # 各状态动画帧
│   │   └── props/          # 道具（斧头、电脑等）
│   └── styles.css
└── hooks/
    └── agent-status.json    # 状态Hook配置
```

### 数据流

```
┌─────────────────┐    Hook写入    ┌──────────────┐
│  Claude Code    │ ─────────────► │  状态文件     │
│  Agent Team     │                │  JSON        │
└─────────────────┘                └──────┬───────┘
                                          │ 轮询/SSE
                                          ▼
                                   ┌──────────────┐
                                   │  本地HTTP    │
                                   │  服务器      │
                                   └──────┬───────┘
                                          │
                                          ▼
                                   ┌──────────────┐
                                   │  浏览器UI    │
                                   │  星露谷风格  │
                                   └──────────────┘
```

---

## 状态同步方案

### 方案 A: Hooks 轮询（推荐）

利用 Claude Code 的 hooks 机制，在工具调用前后写入状态文件：

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": ".*",
        "hooks": ["python hooks/update-status.py --event=pre"]
      }
    ],
    "PostToolUse": [
      {
        "matcher": ".*",
        "hooks": ["python hooks/update-status.py --event=post"]
      }
    ]
  }
}
```

状态文件格式：
```json
{
  "agents": [
    {
      "id": "explore-1",
      "name": "Explorer",
      "status": "working",
      "currentTask": "Searching codebase...",
      "lastUpdate": "2026-02-26T10:30:00Z"
    }
  ]
}
```

### 方案 B: MCP Server

创建一个 MCP Server，暴露状态查询接口：

```python
# server/index.py
from mcp.server import Server

server = Server("stardew-team-ui")

@server.tool()
def update_agent_status(agent_id: str, status: str, task: str):
    """Update agent status for UI display"""
    # 广播到 WebSocket 连接的浏览器
    ...

@server.resource("status://agents")
def get_agent_status():
    """Return current agent statuses"""
    ...
```

---

## UI 设计草图

```
┌─────────────────────────────────────────────────────────────┐
│  🌟 Claude Code Team - Stardew Valley Edition              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│    ┌─────────┐    ┌─────────┐    ┌─────────┐              │
│    │  ( idle ) │    │ (working)│    │(thinking)│              │
│    │   🧑‍🌾    │    │   🧑‍🔧    │    │   🧑‍🎓    │              │
│    │  Plan    │    │  Bash    │    │ Explore │              │
│    │          │    │ ██████░░ │    │  ...     │              │
│    └─────────┘    └─────────┘    └─────────┘              │
│                                                             │
│    ┌─────────────────────────────────────────────┐          │
│    │ 📋 Task List                                 │          │
│    │ ├─ [✓] 探索代码库结构                        │          │
│    │ ├─ [▶] 实现 UI 组件                         │          │
│    │ └─ [ ] 测试并部署                           │          │
│    └─────────────────────────────────────────────┘          │
│                                                             │
│    ☀️ Morning - Day 1                    [🎵] [⚙️]          │
└─────────────────────────────────────────────────────────────┘
```

---

## 技术栈

- **前端**: 原生 HTML/CSS/JS + Canvas 或 CSS Sprites
- **后端**: Python (MCP Server) 或 Node.js
- **精灵图**: 自绘或使用 Stardew Valley 风格素材（注意版权）
- **通信**: Server-Sent Events (SSE) 或 WebSocket

---

## 阻碍与风险

| 问题 | 影响 | 解决方案 |
|-----|------|---------|
| Agent Teams 是实验性功能 | API 不稳定 | 等待正式发布 |
| 无官方状态 API | 无法获取实时状态 | Hook 方案绕过 |
| 素材版权 | 不能直接用游戏素材 | 自绘或找开源像素素材 |
| 多 Agent 并发 | 状态文件竞争 | 文件锁或数据库 |

---

## 下一步行动

- [ ] 制作像素风格小人精灵图
- [ ] 实现静态 UI Demo（HTML + CSS）
- [ ] 研究 Claude Code hooks 深度集成
- [ ] 向 Anthropic 提 Feature Request（暴露 Agent 状态 API）

---

## 参考

- Claude Code Agent Teams 文档
- Stardew Valley 美术风格指南
- MCP (Model Context Protocol) 规范